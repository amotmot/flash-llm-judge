[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "runpod-flash-llm-judge"
version = "0.1.0"
description = "Three-endpoint LLM judge running serverless GPUs on Runpod"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = ["runpod-flash"]